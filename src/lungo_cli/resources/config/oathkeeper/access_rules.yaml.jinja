{% set network = config.network %}

- id: app
  match:
    url: 'https://{{ network.hostname }}/app/<.*>'
    methods:
      - GET
      - POST
      - DELETE
      - PATCH
      - PUT
  authenticators:
    - handler: cookie_session
    - handler: anonymous
  authorizer:
    handler: remote_json
  mutators:
    - handler: header
  errors:
    - handler: json

- id: catch-all
  match:
    url: 'https://{{ network.hostname }}/'
  authenticators:
    - handler: noop
  errors:
    - handler: json
